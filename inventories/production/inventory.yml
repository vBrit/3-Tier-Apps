---
all:
  hosts:
    1web-01a:
      ansible_host: "{{Common.BaseNetwork.IPv4}}.{{Common.SiteCode + 1}}.11"
    1web-02a:
      ansible_host: "{{Common.BaseNetwork.IPv4}}.{{Common.SiteCode + 1}}.12"
      #subnet_mask_bits: 24
      gateway: "{{ Target.VMs.SEG_Web_GW }}"
      dns: "{{ Common.DNS }}"
      guest_username: "root"
      guest_password: "{{ Common.Password.VMs }}"
      segment: "{{ Target.vCenter.PortGroup.Web }}"
    1web-03a:
      ansible_host: "{{Common.BaseNetwork.IPv4}}.{{Common.SiteCode + 1}}.13"
      #subnet_mask_bits: 24
      gateway: "{{ Target.VMs.SEG_Web_GW }}"
      dns: "{{ Common.DNS }}"
      guest_username: "root"
      guest_password: "{{ Common.Password.VMs }}"
      segment: "{{ Target.vCenter.PortGroup.Web }}"
    1app-01a:
      ansible_host: "{{Common.BaseNetwork.IPv4}}.{{Common.SiteCode + 2}}.10"
      #subnet_mask_bits: 24
      gateway: "{{ Target.VMs.SEG_App_GW }}"
      dns: "{{ Common.DNS }}"
      guest_username: "root"
      guest_password: "{{ Common.Password.VMs }}"
      segment: "{{ Target.vCenter.PortGroup.App }}"
    1db-01a:
      ansible_host: "{{Common.BaseNetwork.IPv4}}.{{Common.SiteCode + 3}}.10"
      #subnet_mask_bits: 24
      gateway: "{{ Target.VMs.SEG_DB_GW }}"
      dns: "{{ Common.DNS }}"
      guest_username: "root"
      guest_password: "{{ Common.Password.VMs }}"
      segment: "{{ Target.vCenter.PortGroup.DB }}"
