---
all:
  children:
    web:
      hosts:
        web-01b:
        web-02b:
        web-03b:
    db:
      hosts:
        db-01b:
    app:
      hosts:
        app-01b:
  vars:
    ansible_user: "root"
    ansible_password: "{{ Common.Password.VMs }}"